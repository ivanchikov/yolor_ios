import SwiftUI

struct EditToolbarView: View {
    var body: some View {
        HStack {
            Button("Add Stop") {}
            Button("Regenerate") {}
        }
    }
}

#Preview {
    EditToolbarView()
}
